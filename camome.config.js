import { defineConfig } from "@camome/system";

export default defineConfig({
  themes: {
    light: {
      transition: {
        bg: "none",
      },
    },
    dark: {
      transition: {
        bg: "none",
      },
    },
  },
});
